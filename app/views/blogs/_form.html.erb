<%= form_for(blog) do |f| %>
  <% if blog.errors.any? %>
  <h2><%= pluralize(bloh.errors.count, "error") %> prohibited this blog from being saved:</h2>

  <ul>
  <% blog.errors.full_message.each do 
    message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

